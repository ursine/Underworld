

function(conan_message MESSAGE_OUTPUT)
    if(NOT CONAN_CMAKE_SILENT_OUTPUT)
        message(${ARGV${0}})
    endif()
endfunction()


macro(conan_find_apple_frameworks FRAMEWORKS_FOUND FRAMEWORKS FRAMEWORKS_DIRS)
    if(APPLE)
        foreach(_FRAMEWORK ${FRAMEWORKS})
            # https://cmake.org/pipermail/cmake-developers/2017-August/030199.html
            find_library(CONAN_FRAMEWORK_${_FRAMEWORK}_FOUND NAME ${_FRAMEWORK} PATHS ${FRAMEWORKS_DIRS})
            if(CONAN_FRAMEWORK_${_FRAMEWORK}_FOUND)
                list(APPEND ${FRAMEWORKS_FOUND} ${CONAN_FRAMEWORK_${_FRAMEWORK}_FOUND})
            else()
                message(FATAL_ERROR "Framework library ${_FRAMEWORK} not found in paths: ${FRAMEWORKS_DIRS}")
            endif()
        endforeach()
    endif()
endmacro()


function(conan_package_library_targets libraries package_libdir deps out_libraries out_libraries_target build_type package_name)
    unset(_CONAN_ACTUAL_TARGETS CACHE)
    unset(_CONAN_FOUND_SYSTEM_LIBS CACHE)
    foreach(_LIBRARY_NAME ${libraries})
        find_library(CONAN_FOUND_LIBRARY NAME ${_LIBRARY_NAME} PATHS ${package_libdir}
                     NO_DEFAULT_PATH NO_CMAKE_FIND_ROOT_PATH)
        if(CONAN_FOUND_LIBRARY)
            conan_message(STATUS "Library ${_LIBRARY_NAME} found ${CONAN_FOUND_LIBRARY}")
            list(APPEND _out_libraries ${CONAN_FOUND_LIBRARY})
            if(NOT ${CMAKE_VERSION} VERSION_LESS "3.0")
                # Create a micro-target for each lib/a found
                set(_LIB_NAME CONAN_LIB::${package_name}_${_LIBRARY_NAME}${build_type})
                if(NOT TARGET ${_LIB_NAME})
                    # Create a micro-target for each lib/a found
                    add_library(${_LIB_NAME} UNKNOWN IMPORTED)
                    set_target_properties(${_LIB_NAME} PROPERTIES IMPORTED_LOCATION ${CONAN_FOUND_LIBRARY})
                    set(_CONAN_ACTUAL_TARGETS ${_CONAN_ACTUAL_TARGETS} ${_LIB_NAME})
                else()
                    conan_message(STATUS "Skipping already existing target: ${_LIB_NAME}")
                endif()
                list(APPEND _out_libraries_target ${_LIB_NAME})
            endif()
            conan_message(STATUS "Found: ${CONAN_FOUND_LIBRARY}")
        else()
            conan_message(STATUS "Library ${_LIBRARY_NAME} not found in package, might be system one")
            list(APPEND _out_libraries_target ${_LIBRARY_NAME})
            list(APPEND _out_libraries ${_LIBRARY_NAME})
            set(_CONAN_FOUND_SYSTEM_LIBS "${_CONAN_FOUND_SYSTEM_LIBS};${_LIBRARY_NAME}")
        endif()
        unset(CONAN_FOUND_LIBRARY CACHE)
    endforeach()

    if(NOT ${CMAKE_VERSION} VERSION_LESS "3.0")
        # Add all dependencies to all targets
        string(REPLACE " " ";" deps_list "${deps}")
        foreach(_CONAN_ACTUAL_TARGET ${_CONAN_ACTUAL_TARGETS})
            set_property(TARGET ${_CONAN_ACTUAL_TARGET} PROPERTY INTERFACE_LINK_LIBRARIES "${_CONAN_FOUND_SYSTEM_LIBS};${deps_list}")
        endforeach()
    endif()

    set(${out_libraries} ${_out_libraries} PARENT_SCOPE)
    set(${out_libraries_target} ${_out_libraries_target} PARENT_SCOPE)
endfunction()


include(FindPackageHandleStandardArgs)

conan_message(STATUS "Conan: Using autogenerated Findtinyobjloader.cmake")
# Global approach
set(tinyobjloader_FOUND 1)
set(tinyobjloader_VERSION "1.0.6")

find_package_handle_standard_args(tinyobjloader REQUIRED_VARS
                                  tinyobjloader_VERSION VERSION_VAR tinyobjloader_VERSION)
mark_as_advanced(tinyobjloader_FOUND tinyobjloader_VERSION)


set(tinyobjloader_INCLUDE_DIRS "/home/bear/.conan/data/tinyobjloader/1.0.6/_/_/package/1a651c5b4129ad794b88522bece2281a7453aee4/include")
set(tinyobjloader_INCLUDE_DIR "/home/bear/.conan/data/tinyobjloader/1.0.6/_/_/package/1a651c5b4129ad794b88522bece2281a7453aee4/include")
set(tinyobjloader_INCLUDES "/home/bear/.conan/data/tinyobjloader/1.0.6/_/_/package/1a651c5b4129ad794b88522bece2281a7453aee4/include")
set(tinyobjloader_RES_DIRS )
set(tinyobjloader_DEFINITIONS )
set(tinyobjloader_LINKER_FLAGS_LIST
        $<$<STREQUAL:$<TARGET_PROPERTY:TYPE>,SHARED_LIBRARY>:>
        $<$<STREQUAL:$<TARGET_PROPERTY:TYPE>,MODULE_LIBRARY>:>
        $<$<STREQUAL:$<TARGET_PROPERTY:TYPE>,EXECUTABLE>:>
)
set(tinyobjloader_COMPILE_DEFINITIONS )
set(tinyobjloader_COMPILE_OPTIONS_LIST "" "")
set(tinyobjloader_LIBRARIES_TARGETS "") # Will be filled later, if CMake 3
set(tinyobjloader_LIBRARIES "") # Will be filled later
set(tinyobjloader_LIBS "") # Same as tinyobjloader_LIBRARIES
set(tinyobjloader_SYSTEM_LIBS )
set(tinyobjloader_FRAMEWORK_DIRS )
set(tinyobjloader_FRAMEWORKS )
set(tinyobjloader_FRAMEWORKS_FOUND "") # Will be filled later
set(tinyobjloader_BUILD_MODULES_PATHS )

conan_find_apple_frameworks(tinyobjloader_FRAMEWORKS_FOUND "${tinyobjloader_FRAMEWORKS}" "${tinyobjloader_FRAMEWORK_DIRS}")

mark_as_advanced(tinyobjloader_INCLUDE_DIRS
                 tinyobjloader_INCLUDE_DIR
                 tinyobjloader_INCLUDES
                 tinyobjloader_DEFINITIONS
                 tinyobjloader_LINKER_FLAGS_LIST
                 tinyobjloader_COMPILE_DEFINITIONS
                 tinyobjloader_COMPILE_OPTIONS_LIST
                 tinyobjloader_LIBRARIES
                 tinyobjloader_LIBS
                 tinyobjloader_LIBRARIES_TARGETS)

# Find the real .lib/.a and add them to tinyobjloader_LIBS and tinyobjloader_LIBRARY_LIST
set(tinyobjloader_LIBRARY_LIST tinyobjloader)
set(tinyobjloader_LIB_DIRS "/home/bear/.conan/data/tinyobjloader/1.0.6/_/_/package/1a651c5b4129ad794b88522bece2281a7453aee4/lib")

# Gather all the libraries that should be linked to the targets (do not touch existing variables):
set(_tinyobjloader_DEPENDENCIES "${tinyobjloader_FRAMEWORKS_FOUND} ${tinyobjloader_SYSTEM_LIBS} ")

conan_package_library_targets("${tinyobjloader_LIBRARY_LIST}"  # libraries
                              "${tinyobjloader_LIB_DIRS}"      # package_libdir
                              "${_tinyobjloader_DEPENDENCIES}"  # deps
                              tinyobjloader_LIBRARIES            # out_libraries
                              tinyobjloader_LIBRARIES_TARGETS    # out_libraries_targets
                              ""                          # build_type
                              "tinyobjloader")                                      # package_name

set(tinyobjloader_LIBS ${tinyobjloader_LIBRARIES})

foreach(_FRAMEWORK ${tinyobjloader_FRAMEWORKS_FOUND})
    list(APPEND tinyobjloader_LIBRARIES_TARGETS ${_FRAMEWORK})
    list(APPEND tinyobjloader_LIBRARIES ${_FRAMEWORK})
endforeach()

foreach(_SYSTEM_LIB ${tinyobjloader_SYSTEM_LIBS})
    list(APPEND tinyobjloader_LIBRARIES_TARGETS ${_SYSTEM_LIB})
    list(APPEND tinyobjloader_LIBRARIES ${_SYSTEM_LIB})
endforeach()

# We need to add our requirements too
set(tinyobjloader_LIBRARIES_TARGETS "${tinyobjloader_LIBRARIES_TARGETS};")
set(tinyobjloader_LIBRARIES "${tinyobjloader_LIBRARIES};")

set(CMAKE_MODULE_PATH "/home/bear/.conan/data/tinyobjloader/1.0.6/_/_/package/1a651c5b4129ad794b88522bece2281a7453aee4/" ${CMAKE_MODULE_PATH})
set(CMAKE_PREFIX_PATH "/home/bear/.conan/data/tinyobjloader/1.0.6/_/_/package/1a651c5b4129ad794b88522bece2281a7453aee4/" ${CMAKE_PREFIX_PATH})

foreach(_BUILD_MODULE_PATH ${tinyobjloader_BUILD_MODULES_PATHS})
    include(${_BUILD_MODULE_PATH})
endforeach()

if(NOT ${CMAKE_VERSION} VERSION_LESS "3.0")
    # Target approach
    if(NOT TARGET tinyobjloader::tinyobjloader)
        add_library(tinyobjloader::tinyobjloader INTERFACE IMPORTED)
        if(tinyobjloader_INCLUDE_DIRS)
            set_target_properties(tinyobjloader::tinyobjloader PROPERTIES INTERFACE_INCLUDE_DIRECTORIES
                                  "${tinyobjloader_INCLUDE_DIRS}")
        endif()
        set_property(TARGET tinyobjloader::tinyobjloader PROPERTY INTERFACE_LINK_LIBRARIES
                     "${tinyobjloader_LIBRARIES_TARGETS};${tinyobjloader_LINKER_FLAGS_LIST}")
        set_property(TARGET tinyobjloader::tinyobjloader PROPERTY INTERFACE_COMPILE_DEFINITIONS
                     ${tinyobjloader_COMPILE_DEFINITIONS})
        set_property(TARGET tinyobjloader::tinyobjloader PROPERTY INTERFACE_COMPILE_OPTIONS
                     "${tinyobjloader_COMPILE_OPTIONS_LIST}")
        
    endif()
endif()
